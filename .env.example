# DSH Server Configuration
# Copy this file to .env and edit

# DSH listen port (localhost only)
DSH_PORT=3079

# DSH home directory
DSH_HOME=/home/ubuntu/.dsh

# Proxy is configured via DSH Settings → LAN Proxy
# or via cordis.patch.yml in the web profile
